export const CosmicWalletIcon = ({ size }: { size: number }) => {
  return (
    <img
      src={"/logo.png"}
      width={size}
      height={size}
      style={{ borderRadius: "50%" }}
    />
  );
};
